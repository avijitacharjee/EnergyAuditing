<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="energyaudit.example.com.enerngyauditing">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tips"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tips"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EnrgiSavingActivity" />
        <activity android:name=".Db_Firebase" />
        <activity android:name=".RegistrationPage" />
        <activity android:name=".LogInActivity"></activity>
    </application>

</manifest>